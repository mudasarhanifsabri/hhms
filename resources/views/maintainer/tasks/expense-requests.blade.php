<section class="pwa-section"><div class="d-flex justify-content-between align-items-center gap-2"><h3>Expense requests</h3><button type="button" class="btn btn-outline-primary btn-sm" data-bs-toggle="modal" data-bs-target="#expenseRequest">Request office payment</button></div>
@forelse($task->expenseRequests as $expense)<div class="pwa-inspection-item"><strong>{{ $expense->expense_no }} · AED {{ number_format($expense->gross_amount,2) }}</strong><p>{{ $expense->staff_payment_status === 'paid_by_staff' ? 'Paid by me — reimbursement requested' : 'Unpaid — office to pay' }}</p><small>Office status: {{ $expense->approval_status === 'draft' ? 'Awaiting review' : ucfirst($expense->approval_status) }}</small><p><a href="{{ \App\Support\MediaStorage::url($expense->invoice_path) }}" target="_blank" rel="noopener">View invoice</a></p></div>@empty<p class="text-muted">No expense requests. Send the supplier invoice for office review.</p>@endforelse</section>
<div class="modal fade" id="expenseRequest" tabindex="-1"><div class="modal-dialog modal-dialog-scrollable"><form class="modal-content" method="POST" enctype="multipart/form-data" action="{{ route('maintainer.task.expense-request',$task) }}">@csrf<input name="submission_id" type="hidden" value="{{ (string)\Illuminate\Support\Str::uuid() }}"><div class="modal-header"><h5>Expense / payment request</h5><button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button></div><div class="modal-body"><label class="w-100 mb-2">Payment status<select name="payment_status" class="form-select"><option value="unpaid">Unpaid — office to pay supplier</option><option value="paid_by_staff">Paid by me — request reimbursement</option></select></label><label class="w-100 mb-2">Supplier<input class="form-control" name="supplier" maxlength="200" required></label><div class="row"><label class="col-6 mb-2">Invoice date<input class="form-control" type="date" name="expense_date" value="{{ today()->toDateString() }}" required></label><label class="col-6 mb-2">Total AED<input class="form-control" type="number" min="0.01" step="0.01" name="amount" required></label></div><label class="w-100 mb-2">Purpose<textarea class="form-control" name="description" minlength="5" maxlength="700" required></textarea></label><label class="w-100 mb-2">Supplier invoice<input class="form-control" type="file" name="invoice" accept=".pdf,.jpg,.jpeg,.png,.webp" required></label><label class="w-100 mb-2">Payment proof (required if paid by you)<input class="form-control" type="file" name="receipt" accept=".pdf,.jpg,.jpeg,.png,.webp"></label><small>Office verifies the invoice, tax and responsibility, then selects the payment account. This request does not record a payment.</small></div><div class="modal-footer"><button class="btn btn-primary">Send to office</button></div></form></div></div>
