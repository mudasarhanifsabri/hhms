@extends('layouts.app')
@section('content')
@include('admin.accounting.partials.module-nav')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h4 class="mb-1">{{ $account->code }} - {{ $account->name }}</h4><p class="text-muted mb-0">Dynamic statement of account with linked source records.</p></div><a href="{{ route('admin.accounting.chart-of-accounts') }}" class="btn btn-light">Chart of Accounts</a></div>
<div class="card"><div class="card-body border-bottom"><form class="row g-2 align-items-end"><div class="col-md-3"><label class="form-label">From</label><input type="date" name="date_from" value="{{ $from?->toDateString() }}" class="form-control"></div><div class="col-md-3"><label class="form-label">To</label><input type="date" name="date_to" value="{{ $to->toDateString() }}" class="form-control"></div><div class="col-md-2"><button class="btn btn-primary w-100">Apply</button></div><div class="col-md-2"><a href="{{ url('/admin/accounting/chart-of-accounts/'.$account->id.'/statement') }}" class="btn btn-light w-100">Reset</a></div></form></div>
<div class="card-body border-bottom"><div class="row g-3"><div class="col-md-3"><span class="text-muted">Opening Balance</span><h5>AED {{ number_format($openingBalance,2) }}</h5></div><div class="col-md-3"><span class="text-muted">Total Debit</span><h5 class="text-danger">AED {{ number_format($rows->sum('debit'),2) }}</h5></div><div class="col-md-3"><span class="text-muted">Total Credit</span><h5 class="text-success">AED {{ number_format($rows->sum('credit'),2) }}</h5></div><div class="col-md-3"><span class="text-muted">Closing Balance</span><h5>AED {{ number_format($running,2) }}</h5></div></div></div>
<div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="bg-light-subtle"><tr><th>Date</th><th>Source</th><th>Receivable From</th><th>Reference</th><th>Description</th><th class="text-end">Debit</th><th class="text-end">Credit</th><th class="text-end">Balance</th><th>Link</th></tr></thead><tbody>@forelse($rows as $row)<tr><td>{{ \Illuminate\Support\Carbon::parse($row->date)->format('d M Y') }}</td><td>{{ $row->source_type }}</td><td>{{ $row->party }}</td><td>{{ $row->reference ?: '-' }}</td><td>{{ $row->description ?: '-' }}</td><td class="text-end text-danger">{{ $row->debit ? 'AED '.number_format($row->debit,2) : '-' }}</td><td class="text-end text-success">{{ $row->credit ? 'AED '.number_format($row->credit,2) : '-' }}</td><td class="text-end fw-semibold">AED {{ number_format($row->balance,2) }}</td><td>@if($row->url)<a href="{{ $row->url }}" class="btn btn-sm btn-soft-primary">Open</a>@endif</td></tr>@empty<tr><td colspan="9" class="text-center text-muted py-4">No account movements.</td></tr>@endforelse</tbody></table></div></div>
@endsection
