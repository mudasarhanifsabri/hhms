<p class="small text-muted">Starter quantities are suggestions. Review them for your apartments and enter your actual replacement costs before applying.</p>
@foreach($templates as $template)
<details class="card"><summary class="card-header">{{ $template->name }} · {{ count(json_decode($template->rows,true)) }} item types — View / Edit</summary><form method="POST" action="{{ route('admin.inventory.template.update',$template->id) }}">@csrf @method('PUT')<div class="table-responsive"><table class="table table-sm align-middle"><thead><tr><th>Room</th><th>Item</th><th>Required</th><th>Replacement AED</th></tr></thead><tbody>@foreach(json_decode($template->rows,true) as $i=>$row)<tr><td><input class="form-control form-control-sm" name="rows[{{ $i }}][room]" value="{{ $row['room'] }}" aria-label="Room" required></td><td><input class="form-control form-control-sm" name="rows[{{ $i }}][name]" value="{{ $row['name'] }}" aria-label="Item" required></td><td><input class="form-control form-control-sm" name="rows[{{ $i }}][required]" value="{{ $row['required'] }}" type="number" min="0" aria-label="Required quantity" required></td><td><input class="form-control form-control-sm" name="rows[{{ $i }}][replacement_cost]" value="{{ $row['replacement_cost'] }}" type="number" min="0" step="0.01" aria-label="Replacement cost" required></td></tr>@endforeach</tbody></table></div><div class="card-footer text-end"><button class="btn btn-primary btn-sm">Save template changes</button></div></form></details>
@endforeach
