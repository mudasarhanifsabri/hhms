<!doctype html><html><head><meta charset="utf-8"><style>
body{font-family:DejaVu Sans,sans-serif;color:#172b4d;font-size:11px}h1{font-size:24px;margin:0 0 12px}.header{border-bottom:3px solid #6044dd;padding-bottom:15px;margin-bottom:20px}.muted{color:#667085}table{border-collapse:collapse;width:100%;margin:16px 0}th,td{padding:10px;border:1px solid #dce2ea;text-align:left}th{background:#f1f3f8}.amount{text-align:right}.total{font-size:16px;color:#137447}.footer{border-top:1px solid #dce2ea;margin-top:25px;padding-top:10px;font-size:9px}
</style></head><body>
<div class="header"><h1>SECURITY DEPOSIT REFUND RECEIPT</h1><strong>{{ \App\Support\AppSettings::get('invoice_establishment_name','PATTERN VACATION HOMES RENTAL') }}</strong><br>{{ \App\Support\AppSettings::get('invoice_legal_name') }}<br>{{ \App\Support\AppSettings::get('invoice_address') }}<br>TRN: {{ \App\Support\AppSettings::get('invoice_trn') }}</div>
<table><tr><td><strong>Receipt ID</strong><br>{{ $entry->id }}</td><td><strong>Payment date</strong><br>{{ $entry->entry_date?->format('d M Y') }}</td></tr><tr><td><strong>Refund request</strong><br>{{ $entry->refund?->request_no }}</td><td><strong>Booking</strong><br>{{ $entry->booking?->booking_reference }}</td></tr><tr><td><strong>Refunded to</strong><br>{{ $entry->recipient }}</td><td><strong>Unit / Building</strong><br>{{ $entry->booking?->property?->name }} / {{ $entry->booking?->property?->building?->name }}</td></tr></table>
<table><tr><th>Description</th><th class="amount">AED</th></tr><tr><td>Deposit held at refund request</td><td class="amount">{{ number_format((float)$entry->refund?->held_at_request,2) }}</td></tr><tr><td>Approved deductions</td><td class="amount">{{ number_format((float)$entry->refund?->deduction_amount,2) }}</td></tr><tr><td>Approved refundable amount</td><td class="amount">{{ number_format((float)$entry->refund?->refund_amount,2) }}</td></tr><tr class="total"><th>Actual refund in this payment</th><th class="amount">{{ number_format((float)$entry->amount,2) }}</th></tr></table>
<p><strong>Payment method:</strong> {{ $entry->payment_method }}<br><strong>Paid from:</strong> {{ $entry->bankAccount?->name }}<br><strong>Transfer / receipt reference:</strong> {{ $entry->reference }}<br><strong>Recorded by:</strong> {{ $entry->creator?->name }}<br><strong>Approved by:</strong> {{ $entry->refund?->reviewer?->name }}</p>
@if($entry->notes)<p><strong>Notes:</strong> {{ $entry->notes }}</p>@endif
<p class="muted">This receipt records only the refund payment above. It is not a tax invoice. Original booking invoices and guest payment records are unchanged.</p>
<div class="footer">Generated by system {{ now()->format('d M Y H:i') }}. Currency: AED.</div>
</body></html>
