<!doctype html>
<html><head><meta charset="utf-8"><style>
body{font-family:DejaVu Sans,Arial,sans-serif;color:#111827;font-size:9px}.header{border-bottom:3px solid #0b2850;padding-bottom:10px;margin-bottom:12px}.brand{font-size:20px;font-weight:700;color:#0b2850}.title{font-size:14px;font-weight:700}.muted{color:#6b7280}.meta{margin:8px 0 12px;padding:8px;background:#f3f6fb;border:1px solid #d8dee9}table{width:100%;border-collapse:collapse}th,td{border:1px solid #d8dee9;padding:6px;vertical-align:top}th{background:#0b2850;color:#fff}.urgent{color:#b45309;font-weight:700}.expired{color:#dc2626;font-weight:700}.valid{color:#15803d;font-weight:700}.summary{margin-bottom:10px}.summary td{background:#f8fafc;text-align:center;font-size:10px}
</style></head><body>
<div class="header"><div class="brand">PATTERN Vacation Homes Rental</div><div class="title">DTCM Permit Report</div><div class="muted">Generated by System · {{ now()->format('d M Y H:i') }}</div></div>
@php
    $rows=$properties->map(function($property){$permit=$property->dtcmDocuments->first();$days=$permit?->expires_at?today()->diffInDays($permit->expires_at,false):null;$status=!$permit?'missing':(!$permit->expires_at?'no_expiry':($days<0?'expired':($days<=7?'urgent':($days<=30?'expiring':'valid'))));return compact('property','permit','days','status');});
@endphp
<table class="summary"><tr><td><strong>Records</strong><br>{{ $rows->count() }}</td><td><strong>Valid</strong><br>{{ $rows->where('status','valid')->count() }}</td><td><strong>Urgent / Expiring</strong><br>{{ $rows->whereIn('status',['urgent','expiring'])->count() }}</td><td><strong>Expired</strong><br>{{ $rows->where('status','expired')->count() }}</td><td><strong>Missing</strong><br>{{ $rows->where('status','missing')->count() }}</td></tr></table>
<div class="meta"><strong>Filters:</strong> Search: {{ $filters['search'] ?: 'All' }} · Status: {{ $filters['status'] ? str($filters['status'])->headline() : 'All' }} · Generated by System</div>
<table><thead><tr><th>Unit / Building</th><th>Owner</th><th>Permit Number</th><th>Issue Date</th><th>Expiry Date</th><th>Days Remaining</th><th>Status</th><th>Reminder</th></tr></thead><tbody>
@forelse($rows as $row)<tr><td><strong>{{ $row['property']->name }}</strong><br><span class="muted">{{ $row['property']->building?->building_name ?? 'No building' }}</span></td><td>{{ $row['property']->landlord?->name ?? 'Not assigned' }}</td><td>{{ $row['permit']?->reference_no ?? '—' }}</td><td>{{ $row['permit']?->issue_date?->format('d M Y') ?? '—' }}</td><td>{{ $row['permit']?->expires_at?->format('d M Y') ?? '—' }}</td><td>{{ is_null($row['days'])?'—':($row['days']<0?abs((int)$row['days']).' overdue':(int)$row['days'].' days') }}</td><td class="{{ $row['status'] }}">{{ str($row['status'])->replace('_',' ')->headline() }}</td><td>{{ $row['permit']?->expiry_reminder_sent_for?->equalTo($row['permit']?->expires_at)?'Sent':'Not sent' }}</td></tr>@empty<tr><td colspan="8" style="text-align:center">No DTCM permits match the selected filters.</td></tr>@endforelse
</tbody></table></body></html>
